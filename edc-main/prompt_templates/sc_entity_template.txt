Given the following clinical text and an entity extracted from a relational triplet:

Text: {input_text}
Entity: "{query_entity}"
Current Entity Type: "{query_entity_type}"
Entity Type Definition: "{query_entity_type_definition}"

Your task: Select the BEST matching Entity Type from the schema choices below, or choose "None of the above" if no choice is a good semantic match.

IMPORTANT: Your answer must be a SINGLE LETTER ONLY (e.g., A, B, C...). Do not output any words, explanation, or punctuation — just the letter.

Choices:
{choices}
